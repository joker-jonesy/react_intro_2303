import React from "react";

function Nav(){
    return(
        <nav>
            <a href="#">HOME</a>
            <a href="#">ABOUT</a>
            <a href="#">Contact</a>
        </nav>
    )
}

export default Nav;